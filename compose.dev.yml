services:
  dev-db:
    image: postgres:18.3
    container_name: luckymaze-postgres-dev
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: d4vpas8w0rd13!!!
      POSTGRES_DB: luckymaze-dev
    ports:
      - "3135:5432"
    volumes:
      - postgres-data-dev:/var/lib/postgresql

volumes:
  postgres-data-dev:
